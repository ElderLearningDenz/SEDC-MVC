@using BurgerAppDemo.Models.Domain
@model List<Burger>

<h1>Burgers:</h1>

<ul>
    @for (int i = 0; i < Model.Count; i++)
        {
            <li>@Model.ElementAt(i).Name -- Price: @Model.ElementAt(i).Price DEN.</li>
        }
</ul>

